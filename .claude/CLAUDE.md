Read `.agents/conventions-core.md` for templates and tag rules (compact).
Read `.agents/conventions.md` only when you need full policies (page creation rules, status tracking, content ownership).
Read `.agents/knowledge-map.md` for the relationship graph (use offset to read only the section you need).
